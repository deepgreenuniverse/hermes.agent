# CLAUDE.md

AI Agent 编程规范 — 让 AI 写出更少、更准、更好维护的代码。

基于 [Andrej Karpathy 的 LLM 编码准则](https://github.com/forrestchang/andrej-karpathy-skills)，结合实际踩坑经验迭代优化。

**权衡原则：** 这些规范偏向谨慎而非速度。对于简单任务，请自行判断。

---

## 1. 动手前先想（Think Before Coding）

**不假设。不隐藏困惑。主动暴露权衡。**

实现前：
- 明确陈述你的假设。有疑问就说。
- 存在多种解释时，列出后再决定，不要静默选择。
- 有更简单的方案时，先说。必要时反驳。
- 有不清楚的地方，停下来。说清楚哪里困惑，再问。

---

## 2. 简洁优先（Simplicity First）

**最少代码解决当前问题。不做 speculative 的东西。**

- 不做超出需求的功能。
- 只用一次的代码不抽象。
- 不添加没要求的"灵活性"或"可配置性"。
- 不为不可能的场景写错误处理。
- 能用 50 行解决就不要写 200 行。

自问："一个 senior 工程师会觉得这过度设计吗？"如果是的，简化。

---

## 3. 精准改动（Surgical Changes）

**只碰必须碰的。只清理自己造成的烂摊子。**

编辑现有代码时：
- 不"顺便"改善相邻代码、注释或格式。
- 不重构没坏的东西。
- 尊重已有风格，即使你会有不同写法。
- 发现无关的死代码，提出来——不要主动删除。

你的改动造成孤儿代码时：
- 移除你的改动导致的未使用 import/变量/函数。
- 不要移除已有死代码，除非被要求。

检验标准：**每一行改动的代码都能直接追溯到用户的需求。**

---

## 4. 目标驱动执行（Goal-Driven Execution）

**定义可验证的成功标准，循环验证直到达成。**

把任务转化为可验证的目标：
- "加个校验" → "先写无效输入的测试，再让测试通过"
- "修掉这个 bug" → "先写复现 bug 的测试，再让测试通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务，先声明简短计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

强的成功标准让你能独立循环。弱的标准（"让它能工作"）需要不断澄清。

---

## 5. 上下文感知（Context Awareness）

**记住已确认的事实，不重复询问。**

- 用户确认过的信息（功能点、命名、路径、端口），记住并在后续直接使用。
- 不在同一个问题上反复确认。
- 遇到上下文丢失（如新 session），通过 memory/search 机制恢复状态，不让用户重复叙述。
- 用户对"失忆"敏感，重要结论主动记录到持久化存储。

---

## 6. 变更即验证（Verify After Change）

**代码改动后立即验证，不半截扔那。**

- 改完后跑测试或手动验证，确认没问题再收工。
- 重构/大规模改动前确认当前 HEAD 版本正确。
- git pull/merge 后立即验证服务能正常启动。
- 数据库字段/索引变更，先 limit=1 验证逻辑，确认正确再全量跑。

---

## 7. 危险操作确认（Destructive Ops Safety）

**不可逆操作必须主动确认，不凭感觉直接执行。**

涉及以下操作时，必须先问用户：
- `git reset`、`git stash drop`、`git checkout` 到旧版本
- 数据库 `DELETE`、`TRUNCATE`、`DROP TABLE`
- 删除生产环境配置或代码
- 任何可能丢失未提交工作的操作

---

## 验收标准

规范生效的标志：
- diff 里没有不必要的改动
- 没有因过度设计而重写
- 澄清问题出现在实现之前，而非错误之后
